{%- for host in groups['zookeeper'] %}
server.{{ zk_id }}={{ hostvars[host]['ansible_' + interface]['ipv4']['address'] }}:{{ zookeeper_leader_port }}:{{ zookeeper_election_port }}
{% endfor -%}

tickTime=2000
dataDir=/var/lib/zookeeper
clientPort=2181
